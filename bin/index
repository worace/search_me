#!/usr/bin/env ruby

require File.join(__dir__, "..", "lib", "search_me")
require "yaml"

session = SearchMe::RequestSession.new(ARGV[0])
session.build_index!
index_path = File.join(__dir__, "..", "indices", "main.yml")
File.open(index_path, 'w') { |file| file.write(session.index.to_yaml) }
